TIMBR — LOGO EXPORT
Wordmark font: OUTFIT LIGHT (weight 300), wide letter-spacing. Free Google Font.
The "-outlined" SVGs have the wordmark converted to vector paths (no font needed) — use for print/sharing.
Icon is pure vector (no font). All SVGs are transparent. Gradient: #ff2f6d -> #ff5a3d -> #ff8a1f -> #ffb000 (left->right).
/svg  vector  ·  /png  transparent raster
